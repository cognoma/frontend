branches:
  ignore:
    - gh-pages

test:
  override:
    - gulp views #rebuild angular template cache 
    - npm run unit

deployment:
    production:
        branch: master
        commands:
            - npm run build
            - aws s3 sync build s3://$AWS_S3_BUCKET_PATH --delete